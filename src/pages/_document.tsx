import { Head, Html, Main, NextScript } from 'next/document';

const Document = () => (
  <Html lang="en">
    <Head />
    <body className="loading">
      <Main />
      <NextScript />
    </body>
  </Html>
);

export default Document;
